// Este arquivo foi refatorado para não usar o Prisma diretamente no frontend
// Usar a API em vez disso

const createTemporaryUserForDevelopment = () => {
  // Isso é apenas para ter algo a mostrar durante o desenvolvimento
  console.log('Usando usuário temporário para desenvolvimento');
};

export { createTemporaryUserForDevelopment }; 